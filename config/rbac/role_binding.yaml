apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: gpu-isolation-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: gpu-isolation-manager-role
subjects:
  - kind: ServiceAccount
    name: gpu-isolation-manager
    namespace: gpu-isolation-system
